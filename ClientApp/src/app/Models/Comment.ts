// export class Post {
//   constructor(public title: string, public body: string, public id: number, public authorId: string, public img: string) {};
// }

// export class CreatePost {
//   static create(event: Post) {
//     return new Post(event.title, event.body, event.id, event.authorId, event.img);
//   }
// }